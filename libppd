#!/bin/bash

export CFLAGS=-"O2"
export CXXFLAGS="-O2"

#HOLD_TUCANA
PKG_VER=2.0.0
URL=https://github.com/OpenPrinting/libppd/releases/download/$PKG_VER/libppd-$PKG_VER.tar.xz
TAR=$(echo $URL | sed -r 's|(.*)/||')
DIR=$(echo $TAR | sed 's|.tar.*||g')
PACKAGE=$(echo $DIR | sed 's|-[^-]*$||g')

# Get Package

cd /blfs/builds
wget $URL
tar -xvf $TAR
cd $DIR
# Fix for CVE-2024-47175
wget https://www.linuxfromscratch.org/patches/blfs/svn/libppd-2.0.0-upstream_fixes-1.patch
# Build
patch -Np1 < ../libppd-2.0.0-upstream_fixes-1.patch

./configure --prefix=/usr                   \
            --disable-static                \
            --with-cups-rundir=/run/cups    \
            --enable-ppdc-utils             \
            --docdir=/usr/share/doc/libppd


make -j22


# Install
sudo make DESTDIR=/pkgs/$PACKAGE install
sudo make install
cd /pkgs



sudo echo "libcupsfilters" > /pkgs/$PACKAGE/depends
sudo echo "" > /pkgs/$PACKAGE/make-depends
sudo tar -cvzpf $PACKAGE.tar.xz $PACKAGE
sudo cp $PACKAGE.tar.xz /finished


cd /blfs/builds
sudo rm -r $DIR


